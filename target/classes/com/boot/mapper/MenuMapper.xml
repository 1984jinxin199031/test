<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.MenuDao">


    <resultMap id="MenuResultMap" type="com.boot.domain.Menu">
        <id property="id" column="id"/>
        <result property="pattern" column="pattern"/>
        <collection property="roles" ofType="com.boot.domain.Role">
            <id column="rid" property="id"/>
            <result column="rname" property="name"/>
            <result column="namechn" property="namechn"/>
        </collection>
    </resultMap>

    <select id="findByPattern" resultMap="MenuResultMap">
        select m.*, r.id as rid, r.name as rname, r.namechn as namechn
        from menu m
        left join menu_role mr on m.`id` = mr.`mid`
        left join role r on r.`id` = mr.`rid`
        where m.pattern = #{url}
    </select>

    <select id="getAllMenu" resultMap="MenuResultMap">
        select m.*, r.id as rid, r.name as rname, r.namechn as namechn
        from menu m
                 left join menu_role mr on m.`id` = mr.`mid`
                 left join role r on r.`id` = mr.`rid`
    </select>
</mapper>